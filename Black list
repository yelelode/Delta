-- =========================================================
-- 🚫 TXR HUB CLOUD BLACKLIST ENGINE
-- =========================================================
if not game:IsLoaded() then
    game.Loaded:Wait()
end

local Players = game:GetService("Players")
local lp = Players.LocalPlayer or Players.PlayerAdded:Wait()

local BLACKLISTED_IDS = {
    [10097226859] = true,
    -- [11542536882] = true,
}

-- Check if current player is blacklisted
if BLACKLISTED_IDS[lp.UserId] then
    task.spawn(function()
        local chatMsg = "Sorry yaar Ashu papa"
        
        -- Safe Chat Dispatch
        pcall(function()
            local TextChatService = game:GetService("TextChatService")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            
            if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
                local channel = TextChatService:WaitForChild("TextChannels", 2):WaitForChild("RBXGeneral", 2)
                if channel then
                    channel:SendAsync(chatMsg)
                end
            else
                local chatEvent = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
                if chatEvent and chatEvent:FindFirstChild("SayMessageRequest") then
                    chatEvent.SayMessageRequest:FireServer(chatMsg, "All")
                end
            end
        end)
        
        task.wait(0.5)
        lp:Kick("\n\n[TXR HUB SECURITY]\n( Chal nikal )\n")
    end)
    
    return true -- Main script ko signal dega ki user blacklisted hai
end

return false -- User clean hai
