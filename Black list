-- =========================================================
-- 🚫 TXR HUB CLOUD BLACKLIST ENGINE
-- =========================================================
if not game:IsLoaded() then
    game.Loaded:Wait()
end

local Players = game:GetService("Players")
local lp = Players.LocalPlayer or Players.PlayerAdded:Wait()

-- =========================================================
-- 🎯 TARGET DATABASE (Direct ID daalne ke ready slots)
-- =========================================================
local BLACKLISTED_USERS = {
    10097226859, -- Pehla Target (Active)

    -- 👇 Achanak kisi ko add karna ho to bas '--' hata kar ID daal do:
    -- 11542536882,
    -- 7976332217,
    -- 11414201166, -- Quick Slot 1
    -- 00000000000, -- Quick Slot 2
    -- 00000000000, -- Quick Slot 3
    -- 00000000000, -- Quick Slot 4
    -- 00000000000, -- Quick Slot 5
    -- 00000000000, -- Quick Slot 6
    -- 00000000000, -- Quick Slot 7
    -- 00000000000, -- Quick Slot 8
}

-- Auto-check logic (Number aur String dono format match karega)
local isBlacklisted = false
local currentId = tostring(lp.UserId)

for _, id in ipairs(BLACKLISTED_USERS) do
    if tostring(id) == currentId and tostring(id) ~= "00000000000" then
        isBlacklisted = true
        break
    end
end

-- Blacklist Action Trigger
if isBlacklisted then
    task.spawn(function()
        local chatMsg = "Sorry yaar Ashu papa"
        
        -- Safe Chat Dispatch
        pcall(function()
            local TextChatService = game:GetService("TextChatService")
            local ReplicatedStorage = game:GetService("ReplicatedStorage")
            
            if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
                local channel = TextChatService:WaitForChild("TextChannels", 2):WaitForChild("RBXGeneral", 2)
                if channel then
                    channel:SendAsync(chatMsg)
                end
            else
                local chatEvent = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
                if chatEvent and chatEvent:FindFirstChild("SayMessageRequest") then
                    chatEvent.SayMessageRequest:FireServer(chatMsg, "All")
                end
            end
        end)
        
        task.wait(1) -- Replication safe delay
        lp:Kick("\n\n[TXR HUB SECURITY]\n( Chal nikal )\n")
    end)
    
    return true
end

return false
