-- ==============================================================================
-- 📡 TXR ROBUST LOGGER (SAFE SIZE & RATE-LIMIT PROOF - FIXED) 📡
-- ==============================================================================

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local StarterGui = game:GetService("StarterGui")
local UserInputService = game:GetService("UserInputService")
local lp = Players.LocalPlayer

-- Discord block bypass proxy
local ORIGINAL_URL = "https://discord.com/api/webhooks/1541722782310604820/L3kjYivqBHkOtU5HZJj7pNFJGyHkBHBaVZPg0AY-QBAJFf_SG3cu8TEcMaRjUuRTiyD1"
local WEBHOOK_URL = ORIGINAL_URL:gsub("https://discord.com", "https://webhook.lewisakura.moe")
local TRACKER_FILE = "TXR_Tracker_Data.json"

local function sendNotification(title, text)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = 4
        })
    end)
end

task.spawn(function()
    local trackerData = {
        Executions = 0,
        FirstSeen = os.time()
    }

    if isfile and readfile and isfile(TRACKER_FILE) then
        pcall(function()
            local decoded = HttpService:JSONDecode(readfile(TRACKER_FILE))
            if type(decoded) == "table" then
                trackerData.Executions = decoded.Executions or 0
                trackerData.FirstSeen = decoded.FirstSeen or os.time()
            end
        end)
    end

    trackerData.Executions = trackerData.Executions + 1

    if writefile then
        pcall(function()
            writefile(TRACKER_FILE, HttpService:JSONEncode(trackerData))
        end)
    end

    -- Device & Platform Detection
    local myDevice = "PC"
    if UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled then
        myDevice = "Mobile"
    elseif UserInputService.TouchEnabled and UserInputService.KeyboardEnabled then
        myDevice = "Hybrid"
    end

    -- Fast Executor Detection
    local executorName = (identifyexecutor and identifyexecutor()) or (getexecutorname and getexecutorname()) or "Unknown Executor"

    -- Ping
    local pingMs = "N/A"
    pcall(function() 
        pingMs = math.floor(lp:GetNetworkPing() * 1000) .. "ms" 
    end)

    -- Compact Roster Generator (Safe 1024-Char Bounds)
    local allPlayers = Players:GetPlayers()
    local totalCount = #allPlayers
    local rosterSummary = ""
    local maxDisplay = math.min(totalCount, 15) -- Max 15 to strictly prevent Embed limit crash

    for i = 1, maxDisplay do
        local p = allPlayers[i]
        local pAge = p.AccountAge or 0
        local pPrem = (p.MembershipType == Enum.MembershipType.Premium) and "★" or "-"
        rosterSummary = rosterSummary .. string.format("[%d] %s (@%s) | ID: %d | Age: %dd | Prem: %s\n", i, p.DisplayName, p.Name, p.UserId, pAge, pPrem)
    end

    if totalCount > 15 then
        rosterSummary = rosterSummary .. string.format("... and %d more players.", totalCount - 15)
    end

    local fieldsList = {
        {
            ["name"] = "👤 User & Client Info",
            ["value"] = string.format("**User:** %s (@%s)\n**ID:** `%d` | **Age:** %d Days\n**Device:** %s | **Executor:** `%s`\n**Ping:** %s | **Total Loads:** %d", lp.DisplayName, lp.Name, lp.UserId, lp.AccountAge, myDevice, executorName, pingMs, trackerData.Executions),
            ["inline"] = false
        },
        {
            ["name"] = "🌐 Server Info",
            ["value"] = string.format("**Place ID:** `%s`\n**Job ID:** `%s`\n**Online Players:** %d", tostring(game.PlaceId), (game.JobId ~= "" and game.JobId or "N/A"), totalCount),
            ["inline"] = false
        },
        {
            ["name"] = string.format("👥 Server Players (%d)", totalCount),
            ["value"] = "```yaml\n" .. (rosterSummary ~= "" and rosterSummary or "No players found") .. "```",
            ["inline"] = false
        },
        {
            ["name"] = "⚡ Direct Teleport Code",
            ["value"] = "```lua\ngame:GetService('TeleportService'):TeleportToPlaceInstance(" .. tostring(game.PlaceId) .. ", '" .. tostring(game.JobId) .. "', game.Players.LocalPlayer)\n```",
            ["inline"] = false
        }
    }

    local webhookPayload = {
        ["username"] = "TXR TELEMETRY",
        ["avatar_url"] = "https://i.imgur.com/8N4kHwF.png",
        ["embeds"] = {
            {
                ["title"] = "🚨 SCRIPT EXECUTION LOG 🚨",
                ["color"] = 65535,
                ["fields"] = fieldsList,
                ["footer"] = {
                    ["text"] = "TXR Secure Logger"
                },
                ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ")
            }
        }
    }

    local requestFunc = (syn and syn.request) or (http and http.request) or http_request or request or (fluxus and fluxus.request)
    
    if requestFunc then
        local success, res = pcall(function()
            return requestFunc({
                Url = WEBHOOK_URL,
                Method = "POST",
                Headers = {
                    ["Content-Type"] = "application/json"
                },
                Body = HttpService:JSONEncode(webhookPayload)
            })
        end)

        if success and res and (res.StatusCode == 200 or res.StatusCode == 204) then
            sendNotification("TXR Logger", "✅ Logs sent to Discord!")
        else
            -- Backup Direct Call if Proxy fails
            pcall(function()
                requestFunc({
                    Url = ORIGINAL_URL,
                    Method = "POST",
                    Headers = {
                        ["Content-Type"] = "application/json"
                    },
                    Body = HttpService:JSONEncode(webhookPayload)
                })
            end)
            local code = res and res.StatusCode or "Err"
            sendNotification("TXR Logger", "⚠️ Response: " .. tostring(code))
        end
    else
        sendNotification("TXR Logger", "❌ Executor lacks HTTP Request capability.")
    end
end)
