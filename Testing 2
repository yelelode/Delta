-- =========================================================
-- ⚡ TXR AUTO PROPS SPAWN (ULTRA-FAST MIXED RESTORE ENGINE) ⚡
-- =========================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local StarterGui = game:GetService("StarterGui")
local lp = Players.LocalPlayer

local function sendNotif(title, text)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = 2
        })
    end)
end

-- Purana loop aur connections reset karo
if _G.TXR_AutoSpawnConnection then
    pcall(function() _G.TXR_AutoSpawnConnection:Disconnect() end)
    _G.TXR_AutoSpawnConnection = nil
end

_G.TXR_AutoSpawnActive = true
sendNotif("⚡ AUTO SPAWN", "Exact-Identity & Mixed Spawner Active!")

-- 🥷 500 Props Error UI Suppressor
local function suppressPropMenu()
    pcall(function()
        local pGui = lp:FindFirstChild("PlayerGui")
        if pGui then
            for _, v in ipairs(pGui:GetDescendants()) do
                if v:IsA("TextLabel") and string.find(string.lower(v.Text), "props") and string.find(v.Text, "500") then
                    local parentFrame = v
                    for i = 1, 6 do
                        if parentFrame.Parent and parentFrame.Parent:IsA("Frame") then
                            parentFrame = parentFrame.Parent
                            if parentFrame.Visible then
                                parentFrame.Visible = false
                            end
                        end
                    end
                end
            end
        end
    end)
end

-- Player ke active props count karo
local function getAllMyProps()
    local props = {}
    local com = workspace:FindFirstChild("WorkspaceCom")
    if com then
        for _, folder in ipairs(com:GetChildren()) do
            for _, ch in ipairs(folder:GetChildren()) do
                if string.find(ch.Name, "Prop") and string.find(ch.Name, lp.Name) then
                    table.insert(props, ch)
                end
            end
        end
    end
    return props
end

-- Mode ke hisaab se target count calculate karo
local function getModeTargetLimit()
    if _G.TXR_ActiveModes and _G.TXR_ActiveModes["Chair"] then
        return 0
    end

    local perTarget = 0
    if _G.TXR_ActiveModes then
        if _G.TXR_ActiveModes["Robot"] then perTarget = perTarget + 4 end
        if _G.TXR_ActiveModes["MultiRobot"] then perTarget = perTarget + 2 end
        if _G.TXR_ActiveModes["HeadBang"] then perTarget = perTarget + 2 end
        if _G.TXR_ActiveModes["Ring"] then perTarget = perTarget + 8 end
        if _G.TXR_ActiveModes["Orbit"] then perTarget = perTarget + 12 end
    end

    if perTarget == 0 then perTarget = 6 end

    local targets = _G.TXR_SelectedTargets or 1
    return math.clamp(perTarget * targets, 2, 30)
end

-- 🔍 Check karega ki kaunse slot ka prop gayab hua hai (Guns, Robots, etc.)
local function getMissingSlotRequirements()
    local missingList = {}
    if not _G.TXR_SlotPropMemory then return missingList end

    for slotKey, data in pairs(_G.TXR_SlotPropMemory) do
        if not data.Remote or not data.Remote.Parent or not data.Remote.Parent.Parent then
            table.insert(missingList, {
                Slot = slotKey,
                PropName = data.PropName or (_G.TXR_CustomPropName or "ScreensRobotBig"),
                Category = data.Category or (_G.TXR_CustomPropCategory or "Screens")
            })
        end
    end
    return missingList
end

local isSpawningLocked = false
local lastSpawnActionTime = 0

-- ⚡ PRECISION SYNC & IDENTITY RESTORE LOOP
_G.TXR_AutoSpawnConnection = RunService.Heartbeat:Connect(function()
    if not _G.TXR_AutoSpawnActive then return end

    local targetLimit = getModeTargetLimit()
    if targetLimit == 0 then return end

    local now = os.clock()
    local myProps = getAllMyProps()
    local currentCount = #myProps

    suppressPropMenu()

    -- Exact count hone par lock release karo
    if currentCount >= targetLimit then
        isSpawningLocked = false
        return
    end

    -- Trigger recovery jab count limit se kam ho
    if currentCount < targetLimit and not isSpawningLocked and (now - lastSpawnActionTime > 0.25) then
        isSpawningLocked = true
        lastSpawnActionTime = now

        task.spawn(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local root = char and char:FindFirstChild("HumanoidRootPart")
            local backpack = lp:FindFirstChild("Backpack")

            if not char or not hum or not root or hum.Health <= 0 then 
                isSpawningLocked = false 
                return 
            end

            local re = ReplicatedStorage:FindFirstChild("RE")
            local clearRemote = re and re:FindFirstChild("1Clea1rTool1s")
            local missingSlots = getMissingSlotRequirements()

            -- CASE A: Ek bhi prop nahi bacha (Fresh Starter Prop)
            if currentCount == 0 then
                local defaultProp = _G.TXR_CustomPropName or "ScreensRobotBig"
                local defaultCat = _G.TXR_CustomPropCategory or "Screens"

                pcall(function()
                    if clearRemote then
                        clearRemote:FireServer("RequestingPropName", defaultProp, defaultCat)
                    end
                end)

                local tool = char:FindFirstChild("PropMaker") or (backpack and backpack:FindFirstChild("PropMaker"))
                local wasAlreadyEquipped = (tool and tool.Parent == char)

                if tool then
                    if not wasAlreadyEquipped then
                        tool.Parent = char
                        RunService.Heartbeat:Wait()
                    end

                    local toolRemote = tool:FindFirstChild("Tool_PropMake")
                    if toolRemote then
                        local spawnPos = root.Position + (root.CFrame.LookVector * 3)
                        pcall(function() toolRemote:FireServer(workspace.Terrain, spawnPos) end)
                    end

                    if not wasAlreadyEquipped and backpack then
                        tool.Parent = backpack
                    end
                end

                local firstProp = nil
                for i = 1, 35 do
                    local check = getAllMyProps()
                    if #check > 0 then
                        firstProp = check[1]
                        break
                    end
                    task.wait(0.02)
                end

                if firstProp then
                    local dupRemote = firstProp:FindFirstChild("DuplicateProp")
                    if dupRemote then
                        local remainingNeeded = targetLimit - #getAllMyProps()
                        for i = 1, remainingNeeded do
                            task.spawn(function()
                                pcall(function() dupRemote:InvokeServer() end)
                            end)
                            task.wait(0.015)
                        end
                    end
                end

            -- CASE B: Specific Props Gayab Hue (Slot-Aware Restore)
            else
                local neededNow = targetLimit - currentCount

                if #missingSlots > 0 then
                    for _, req in ipairs(missingSlots) do
                        pcall(function()
                            if clearRemote then
                                clearRemote:FireServer("RequestingPropName", req.PropName, req.Category)
                            end
                        end)

                        local matchedRemote = nil
                        for _, p in ipairs(myProps) do
                            local d = p:FindFirstChild("DuplicateProp")
                            if d and d.Parent then
                                matchedRemote = d
                                break
                            end
                        end

                        if matchedRemote then
                            task.spawn(function()
                                pcall(function() matchedRemote:InvokeServer() end)
                            end)
                            task.wait(0.02)
                        end
                    end
                else
                    local dupRemote = nil
                    for _, p in ipairs(myProps) do
                        local d = p:FindFirstChild("DuplicateProp")
                        if d and d.Parent then
                            dupRemote = d
                            break
                        end
                    end

                    if dupRemote and neededNow > 0 then
                        for i = 1, neededNow do
                            task.spawn(function()
                                pcall(function() dupRemote:InvokeServer() end)
                            end)
                            task.wait(0.015)
                        end
                    end
                end
            end

            task.wait(0.18)
            isSpawningLocked = false
        end)
    end
end)
